{
    "configurations": [
        
      {
        "name": "Python: Current File (with PYTHONPATH)",
        "type": "debugpy",
        "request": "launch",
        "program": "${file}",
        "env": {
          "PYTHONPATH": "${Sudoku-solver}"
        }
      }
    ]
  }
  